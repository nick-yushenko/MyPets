@if (ViewBag.hasAttendance)
{
    <div class="statistic-block withShadow">
        <div class="info-btn">
            <img src="~/Content/img_back/icons/info-icon.svg" alt="">
        </div>
        <div class="info-block">
            Данная статистика заполняется преподавателем. Число занятий указано за весь период обучения в Mr Simon Academy
        </div>
        <h2 class="statistic-block__title">Посещаемость</h2>
        <div class="statistic-block__info">
            <span>Всего занятий: <span class="primary" id="totalAmountText">32</span></span>
            <span>Пропущено: <span class="primary" id="missedText">2</span></span>
        </div>
        <div class="diagram-wrap attendance">
            <svg class="statistic-block__svg">
                <!-- data-count - общее количество -->
                <circle class="statistic-block__circle total" data-count="@ViewBag.total" fill="transparent" stroke="#E7F7FB"
                        stroke-width="20" cx="80" cy="80" r="70" />
                <!-- data-count - сколько посещено -->
                <circle class="statistic-block__circle visited" data-count="@ViewBag.visited" fill="transparent" stroke="#7FD3B0"
                        stroke-width="20" cx="80" cy="80" r="70" />

            </svg>
            <span class="percent js-attendance-percent"></span>
            <!--
            <div class="emoji-rating">
                @if (ViewBag.percentEmoji <= 20)
                {
                    <img src="~/Content/img_back/emoji-rating/emoji-1.svg" alt="">
                }
                @if (ViewBag.percentEmoji > 20 && ViewBag.percentEmoji <= 40)
                {
                    <img src="~/Content/img_back/emoji-rating/emoji-2.svg" alt="">
                }
                @if (ViewBag.percentEmoji > 40 && ViewBag.percentEmoji <= 60)
                {
                    <img src="~/Content/img_back/emoji-rating/emoji-3.svg" alt="">
                }
                @if (ViewBag.percentEmoji > 60 && ViewBag.percentEmoji <= 80)
                {
                    <img src="~/Content/img_back/emoji-rating/emoji-4.svg" alt="">
                }
                @if (ViewBag.percentEmoji > 80 && ViewBag.percentEmoji <= 100)
                {
                    <img src="~/Content/img_back/emoji-rating/emoji-5.svg" alt="">
                }

            </div>
            -->
        </div>

    </div>

}

